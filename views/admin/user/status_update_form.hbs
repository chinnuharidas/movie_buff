{{#ifCond status '==' 'enable' }}
<form method="post" id="remarksForm" action="/admin/user/enable/{{ user._id }}">
{{/ifCond }}
{{#ifCond status '==' 'disable' }}
<form method="post" id="remarksForm" action="/admin/user/disable/{{ user._id }}">
{{/ifCond }}
    <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">
            {{#ifCond status '==' 'enable' }}
                Enable Remarks
            {{/ifCond }}

            {{#ifCond status '==' 'disable' }}
                Disable Remarks
            {{/ifCond }}
        </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
         <div class="form-group">
            <label for="exampleFormControlTextarea1">Remarks</label>
            <textarea class="form-control" name="status_update_remarks" id="exampleFormControlTextarea1" rows="3"></textarea>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="submit" onclick="return validateRemarksForm()" class="btn btn-primary">Submit</button>
    </div>
</form>